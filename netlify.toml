[build]
  command = "npm run build"
  # Next.js runtime publishes the build output; must differ from the repo root.
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Next.js runtime for Netlify (auto-installed if omitted, declared here for clarity).
[[plugins]]
  package = "@netlify/plugin-nextjs"
